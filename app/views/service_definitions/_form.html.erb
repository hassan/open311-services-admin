<%= form_for(@service_definition) do |f| %>
  <% if @service_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_definition.errors.count, "error") %> prohibited this service_definition from being saved:</h2>

      <ul>
      <% @service_definition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr>
      <th><%= f.label :service_id %></th>
      <td><%= f.number_field :service_id %></td>
    </tr>
    <tr>
      <th><%= f.label :service_code %></th>
      <td><%= f.text_field :service_code %></td>
    </tr>
    <tr>
      <td></td>
      <td class="actions"><%= f.submit %></td>
    </tr>
  </table>
<% end %>
